apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx-pod
  name: nginx-pod
  namespace: default
spec:
  containers:
  - image: fadydevops/webapp:tagname
    name: nginx-pod
    resources: {}
  dnsPolicy: ClusterFirst
